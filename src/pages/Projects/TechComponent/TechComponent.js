import React, { useEffect } from "react"
import "./TechComponent.css"
// import { gsap } from "gsap"

export const TechComponent = (props) => {
   const { image, text } = props

   useEffect(() => {}, [])

   return <div className="tech-component"></div>
}
